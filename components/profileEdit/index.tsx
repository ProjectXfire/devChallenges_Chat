import React from "react";
// Styles
import { SProfileEdit } from "@styles/components/profileEdit";

export const CProfileEdit: React.FC = ({ children }) => {
  return <SProfileEdit>{children}</SProfileEdit>;
};
